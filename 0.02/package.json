{
  "name": "musif",
  "version": "1.0.0",
  "description": "Application that searches SoundCloud API for track, sends it to Echonest API for track analysis then displays visual content that is manipulated by the song data as the song plays",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Lucy Matchett",
  "license": "ISC",
  "dependencies": {
    "echonestjs": "0.0.5",
    "node-soundcloud": "0.0.3",
    "angular": "^1.4.0-rc.1",
    "angular-bootstrap-lightbox": "^0.6.1",
    "body-parser": "^1.5.2",
    "express": "^4.0.0"
  },
  "devDependencies": {}
}
